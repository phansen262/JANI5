<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JANI5"
        tools:targetApi="31">

        <!-- Launcher Activity -->
        <activity
            android:name=".gui.startup.LauncherActivity"
            android:theme="@style/Theme.JANI5"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- New User Activities -->
        <activity android:name=".gui.startup.NewUserActivity"
            android:theme="@style/Theme.JANI5"
            android:exported="true"/>

        <!-- Main Activity -->
        <activity android:name=".gui.main.MainActivity"
            android:theme="@style/Theme.JANI5"
            android:exported="true"/>

        <!-- SNAV Activity -->
        <activity android:name=".gui.snav.SnavActivity"
            android:theme="@style/Theme.JANI5"
            android:exported="true"/>

    </application>

</manifest>